# Optional server-side donation gateway adapter.
# Leave these blank until an authorised provider and adapter are ready.
DONATION_GATEWAY_API_URL=
DONATION_GATEWAY_API_TOKEN=
DONATION_GATEWAY_PROVIDER=
